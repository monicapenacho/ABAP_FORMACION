projection implementation in class ZBP_C_PCFLIGHT unique;
strict ( 2 );
extensible;
use draft;
use side effects;
define behavior for ZC_PCFlight alias Vuelo
extensible
use etag
{
//  use create;
  use update;
//  use delete;

  field (readonly) PlaneTypeID;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}